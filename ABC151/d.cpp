#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef int64_t i6;

#define rep(i, n) for (ll i = 0; i < n; ++i)
#define Sort(x) sort(x.begin(), x.end())
#define Sort_rev(x) sort(x.begin(), x.end(), greater<int>())
#define ALL(v) (v).begin(), (v).end()

//π M_PI
//deg = rad*180/M_PI
ll ans( ll pos[2], char s[a][b]) {
    if () {
        /* code */
    }
}

int main() {
    ll h, w;
    cin >> h >> w;
    char s[h][w];
    rep(i, h) {
        rep(j, w) {
            cin >> s[i][j];
        }
    }

    ll keep[2];
    rep(i, h*w) {
        if ( ) {
            /* code */
        }
    }
    return 0;
}
