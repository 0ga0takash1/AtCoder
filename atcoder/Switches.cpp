#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,m;
    cin >> n >> m;
    int k[10],s[10][10],p[10];
    int on;
    for (int i = 0; i < m; i++){
        cin >> k[i];
        for (int j = 0; j < k[i]; j++){
            cin >> s[i][j];
        }
    }
    for (int i = 0; i < m; i++){
        cin >> p[i];
    }

    for (int i = 0; i < m; i++){
        if ( p[i] == 0 ) {
            
        } else {
            /* code */
        }
    }
}
