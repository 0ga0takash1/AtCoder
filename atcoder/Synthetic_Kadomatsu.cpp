#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    int l[n];
    for (int i = 0; i < n; i++) {
        cin >> l[i];
    }

    int ans = 0;
    
}
